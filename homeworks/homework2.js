/*let personAge = 33

function isAdult(age){
  if(age >= 18){
    return true 
  }

  return false 
} 

let adult = isAdult(personAge)
console.log(adult)


function profile(name, age, likesCoding, iAmAdult){
  return `Hi I am ${name}, I am ${age} years old, and it is ${likesCoding} that I like coding. I am
  a adult: ${iAmAdult}`
}

let myProfile = profile('Nick', personAge, true, adult)
console.log(myProfile)


const square = (number) => {
  return number * number
}
let result = square(10)
console.log(result)


// It is also possible to write like this below
// If there is only one parameter you can omit the parentheses () around the parameter
// You can also remove the keyword "return" and curly brackets {} if there is only one statement

const square = number => number * number

let result = square(10)
console.log(result)*/

/*function calculator(a, b, operator){

  if(operator === 'add'){
    return a + b
  } else if (operator === 'subtract'){
    return a - b
  } else if (operator === 'multiply'){
    return a * b
  } else if (operator === 'divide'){
    return a / b
  } else {
    return 'Invalid operator'
  }
}
let add = calculator(10, 2, 'add')
console.log(add)
let subtract = calculator(10, 2, 'subtract')
console.log(subtract)
let multiply = calculator(10, 2, 'multiply')
console.log(multiply)
let divide = calculator(10, 2, 'divide')
console.log(divide)
let inValid = calculator(10, 2, 'modulus')
console.log(inValid)


function greetWithTime(name, timeOfDay = "morning"){

  return `Good ${timeOfDay}, ${name}!`

}

let greet = greetWithTime('Jane')
console.log(greet)


function profile(name, age, likesCoding){
  return `Hi I am ${name}, I am ${age} years old, and it is ${likesCoding} that I like coding`
}

let myProfile = profile('Jane', 29, true)
console.log(myProfile)

function square(number){

  return number * number

}

let result = square(10)
console.log(result)
*/


/*
function subtract (a, b){
  console.log(a - b)
}

subtract(10,3)


function welcomeUser(name){

  console.log('Welcome ' + name)

}

welcomeUser('Jane')
welcomeUser('Lilly')


function sayHello (){
  console.log('Hello from Node.js')
}

sayHello()*/

